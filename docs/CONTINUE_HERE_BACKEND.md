# TripKing — Session A (backend lane): edge functions & API

> **This is the entry point for the *backend* parallel session.** Companion to `docs/CONTINUE_HERE.md` (the shared map — read its "Running two sessions in parallel — lane ownership" section first), `CLAUDE.md` (working rules), and `docs/PLATFORM_AND_ADMIN_REQUIREMENTS.md` (the spec). The *frontend* session works from `docs/CONTINUE_HERE_FRONTEND.md` — you do not need to read it.

## Your lane (the only paths you touch)

`supabase/**` (functions, `config.toml`, migrations) · `public/docs/openapi.yaml` + `public/docs/openapi.json` · `scripts/test-*.cjs` + `scripts/db.cjs` · `tests/load/**` (if added) · your own section/bullet in `CLAUDE.md` / `docs/CONTINUE_HERE*.md`.

**Never touch `src/**`** — that's the frontend lane. The only cross-lane contract is the `POST /drivers` / `POST /agents` "create my profile" route below; its shape is fixed here and in `docs/CONTINUE_HERE_FRONTEND.md` — don't change it without updating both docs and pinging the other session.

Push protocol: `git pull --rebase origin main` **immediately before** every `git push origin main`. Disjoint lanes ⇒ the rebase always replays cleanly. A rebase conflict ⇒ you crossed lanes — stop, fix the boundary, don't force.

## State (already done, deployed, pushed)

8 edge functions live at `…supabase.co/functions/v1/<name>`: `/admin` (8/8 smoke ✓), `/auth` (phone-OTP, dev-OTP `123456`, real Supabase sessions — 7/7), `/trips` (full lifecycle — 8/8), `/notifications` (6/6), `/vehicles` (CRUD + derived `eligibility_status` — 6/6), `/drivers` + `/agents` (profiles + "create my profile" + location — 22/22, commit `eccde0e`), **`/vacancies` (browse with filters + joins / post with destinations / cancel — 22/22)**. 3 migrations applied (001 reference data; 002 core schema = `users`+`handle_new_user`+`is_admin()`, `drivers`/`trip_managers`/`vehicles`, `trips`/`trip_acceptances`/`trip_executions` with `driver_payout`/`applicant_count` triggers; 003 = `vacancies`+`vacancy_destinations`/`alerts`/`reviews`/`notifications` + admin-write RLS). OpenAPI covers all 8. `supabase/functions/_shared/{cors,timing,supabase}.ts` exist and are reused. The frontend data layer (services/transforms/hooks for all 10 resources) is already in place — your functions must return shapes its transforms accept (joined `select(...)` strings below).

## NEXT STEP → the 2 remaining edge functions, in order: `/alerts` → `/reviews`. Then Phase-4/5 backend. (✅ `/drivers`+`/agents` → `supabase/functions/{drivers,agents}/index.ts` + `scripts/test-drivers.cjs`; ✅ `/vacancies` → `supabase/functions/vacancies/index.ts` + `scripts/test-vacancies.cjs`.)

### The recipe (every remaining edge function — mirror `supabase/functions/trips/index.ts`)

1. **`supabase/functions/<name>/index.ts`** — Deno; `serve(withTiming('<name>', async (req) => { … }))`; handle `corsPreflight`; parse the path with `url.pathname.match(/\/<name>(?:\/(.+))?$/)` → routes at the function root (`GET /` = list, `GET /:id`, `POST /:id/<action>`, etc.); for authed routes read the Bearer token (`req.headers.get('authorization')?.slice(7)`), validate via `db.auth.getUser(token)` → `auth.uid()`, look up the caller's `users.role` (+ `drivers.id` if relevant), and **enforce the same ownership rules the RLS policies in migrations 002/003 encode** (owner / poster / assigned-driver / `role==='admin'`); writes go through the **service-role client** (`serviceClient()` from `../_shared/supabase.ts` — it reads the auto-injected `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`); use joined `select(...)` strings that match the frontend transforms (e.g. `'*, from_city:cities!from_city_id(*), to_city:cities!to_city_id(*), car_type:car_types(label)'`); map Postgres errors with a `pgFail` helper (`23505`→`CONFLICT` 409, `23503`→`IN_USE` 409, `23502`/`23514`/`22P02`→`VALIDATION` 422, else `DB_ERROR` 400). Use `ok(data)` / `fail(code, message, status)` from `../_shared/cors.ts`. Reuse `../_shared/{cors,timing,supabase}.ts`.
2. **`supabase/config.toml`** — add `[functions.<name>]` then `verify_jwt = false` (the function does its own auth; the gateway gate is off).
3. **OpenAPI** — append the paths to `public/docs/openapi.yaml` (compact one-line `post: { tags: […], summary: …, … }` style) **and** to `public/docs/openapi.json` (a small `node -e "const p=JSON.parse(…); Object.assign(p.paths,{…}); p.tags=p.tags.concat([…]); fs.writeFileSync(…)"` script, like the trips/auth/notifications/vehicles appends). Add a tag for the resource.
4. **`scripts/test-<name>.cjs`** — Node smoke test mirroring `scripts/test-vehicles.cjs`: read `<NAME>_API_BASE` (skip cleanly + `exit 0` if unset); get a token via `POST /auth/auth/request-otp` then `POST /auth/auth/verify-otp { phone, otp:'123456', display_name, role }`; exercise the routes (a public read → 200+array, a write without auth → 401, a `404` case, a `403` case where applicable); `process.exit(1)` on any failed check.
5. **Deploy & smoke:** `npx supabase functions deploy <name> --project-ref saxcbebqxgatiktsebxw --no-verify-jwt` then `<NAME>_API_BASE=https://saxcbebqxgatiktsebxw.supabase.co/functions/v1 node scripts/test-<name>.cjs`.
6. **Commit & push:** `feat(<name>): …` (one logical change; review `git diff --stat` — must be only `supabase/`, `public/docs/`, `scripts/`, `tests/load/`) → `git pull --rebase origin main && git push origin main` (the Husky pre-push runs `tsc --noEmit` + `npm run test:run` + `npm run build` — all green; secret-scanning blocks any literal `sb_secret_…` so never commit one).

### Per-resource notes

- **`/drivers` (+`/agents`) — ✅ SHIPPED (commit `eccde0e`)** — `GET /drivers ?current_city_id=&kyc_status=&limit=` (public; joins `home_city`/`current_city`/`vehicles`(summary)), `POST /drivers` ("create my profile" — `user_id = auth.uid()`; body `{ full_name, role:'driver'|'trip_manager', home_city_id, current_city_id, phone, email, profile_photo_url }`; `role:'trip_manager'` creates a `trip_managers` row instead; **idempotent** — returns the existing profile if one exists; syncs `users.role`), `GET /drivers/:id`, `PATCH /drivers/:id` (owner or admin — `full_name`/`email`/`home_city_id`/`current_city_id`/`profile_photo_url`), `PATCH /drivers/:id/location` (owner — `current_city_id`/`current_lat`/`current_lng`/`current_location_at`). `/agents` (= `trip_managers`): `GET /agents ?business_city_id=&kyc_status=&limit=` (public), `POST /agents` (create my agent profile; idempotent; syncs `users.role`), `GET /agents/:id`, `PATCH /agents/:id` (owner or admin — `full_name`/`email`/`business_name`/`business_city_id`/`profile_photo_url`). Frontend service `src/lib/api/services/drivers.ts` already calls `/drivers`, `/drivers/:id`, `/drivers/:id/location`, `/agents/:id` (and Session B adds `createMyDriverProfile`/`createMyAgentProfile` → `POST /drivers`/`POST /agents`); transform `src/lib/api/transforms/driver.ts`. Files: `supabase/functions/drivers/index.ts`, `supabase/functions/agents/index.ts`, `scripts/test-drivers.cjs` (22 checks ✓).
- **`/vacancies` — ✅ SHIPPED** — `GET /vacancies ?current_city_id=&destination_city_id=&status=&driver_id=&limit=` (public; joins `driver`(summary)+`vehicle`(summary)+`current_city`+`vacancy_destinations(city:cities(*))`; `destination_city_id` filter pre-resolves `vacancy_destinations` → `id IN (...)`), `GET /vacancies/:id`, `POST /vacancies` (driver — `current_city_id` required; body incl. `destination_city_ids: string[]` → insert the vacancy then the junction rows, rollback the orphan on a junction-insert failure), `POST /vacancies/:id/cancel` (owning driver/admin — `status='cancelled'`, 409 if already cancelled). Files: `supabase/functions/vacancies/index.ts`, `scripts/test-vacancies.cjs` (22 checks ✓). Service `services/vacancies.ts`; transform `transforms/vacancy.ts`.
- **`/alerts`** — owner-scoped: `GET /alerts` (the caller's), `GET /alerts/:id`, `POST /alerts`, `PATCH /alerts/:id` (incl. `is_active`/`paused_at`), `DELETE /alerts/:id`. `notify_via` is a `text[]` constrained to `{push,sms,email,in_app}`; `car_type_ids` is a `uuid[]`. Service: `services/alerts.ts`; transform: `transforms/alert.ts`.
- **`/reviews`** — `GET /reviews ?trip_id=&ratee_user_id=&direction=` (published readable by all; parties see their own — for the simple version: return `is_published` rows + the caller's own when authed), `POST /reviews` (the rater — `rater_user_id = auth.uid()`; `direction`/`score 1-5`/`comment`/`tag_ids`; unique `(trip_id, direction)`; should ideally check `trip.status='completed'`), `POST /reviews/:id/report` (any authed user — set `is_flagged`+`flag_reason`). Anonymous passenger reviews (`rater_user_id` null) are out of scope for this function — they'd come via a passenger-portal edge function later. Service: `services/reviews.ts`; transforms: `transforms/review.ts` (`transformReview` + `transformNotification`).

### After the 4 → Phase-4 / Phase-5 backend (still backend-lane only — no `src/**`)

- **Phase 4 backend** — admin-ops endpoints: KYC-queue actions on `drivers.kyc_status` (the `pending → docs_submitted → video_pending → approved | rejected | resubmit_required` transitions, admin-only), vehicle-eligibility dashboard data (derive `eligible | expiring_soon | expired` from `year` vs `app_settings.min_vehicle_year` + the expiry windows), translation-manager backend wired to the `languages` lookup, reviews-moderation queue actions (clear/uphold a `is_flagged` review). Same recipe.
- **Phase 5 backend** — server-computed analytics endpoints: agent analytics + admin dashboards. Same recipe.
- **Phase 6 backend** — replace the `X-Admin-Key` stopgap in `supabase/functions/admin/index.ts` with a `role=admin` Bearer check once `public.users` has admin rows; real SMS provider + an `auth_otps` table for `/auth`; rate limiting; RLS-coverage review.

(The frontend session builds the UIs on these later — coordinate the response shapes via OpenAPI, which you own.)

## Known TODOs / gotchas

- **`X-Admin-Key` is a stopgap** — `/admin/*` gates *mutations* on the `ADMIN_API_KEY` env secret (in `.env.development` + set as a Supabase function secret). The `apiClient` doesn't send it, so the `/administration/config` UI's writes 403 until this is replaced with a real `role=admin` Bearer check (Phase 6 backend, above). Reads (public) work.
- **dev-OTP** — `/auth` is in dev mode: `request-otp` returns the code, `verify-otp` accepts `123456` (or any 6-digit). Real SMS provider + an `auth_otps` table is Phase 6 backend.
- **Driver/agent profiles on sign-in** — `verify-otp` creates the `users` row only; the `POST /drivers` / `POST /agents` "create my profile" route (✅ shipped) is how a fresh sign-in becomes a driver/agent. The `/trips` apply→assign→start and `/vehicles` POST paths can now be smoke-tested end-to-end (their `scripts/test-*.cjs` haven't been upgraded to do so yet — optional follow-up: bootstrap a driver via `POST /drivers` in `scripts/test-trips.cjs` / `scripts/test-vehicles.cjs`).
- **Edge-function routing** — function `<name>` is reachable at `…/functions/v1/<name>/…`; routes live at the function root (`GET /` = list, `GET /:id`, …). The `auth` function additionally matches the *last* path segment, so `/auth/me` and `/me` both resolve. (An `api.tripking.in` gateway would map `/<name>/*` → the `<name>` function later.)
- **`supabase/` is outside the app build** — `tsc --noEmit` / ESLint / `vite build` ignore it, so your edge-function changes don't affect the `tsc/test/build` gate; verification for them is the `scripts/test-<name>.cjs` smoke against the deployed function. (This is also why your lane and the frontend lane never break each other's pre-push gate.)

## Keys & access (full details in `CLAUDE.md` — never ask the user for tokens)

Supabase project `tripking` ref `saxcbebqxgatiktsebxw`. SQL / migrations via `node scripts/db.cjs "<SQL>"` or `node scripts/db.cjs --file supabase/migrations/<NNN>_<name>.sql` (Management API; access token from the Supabase CLI's Windows Credential Manager entry — if missing, `npx supabase login`). Deploy with `npx supabase functions deploy <name> --project-ref saxcbebqxgatiktsebxw --no-verify-jwt`. Service-role secret key + legacy `service_role` JWT in `.env.development` (`SUPABASE_SERVICE_ROLE_KEY`) — never commit the literal value (GitHub push-protection blocks `sb_secret_…`); edge functions get `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` auto-injected so no secret-setting is needed there. `ADMIN_API_KEY` in `.env.development` + set as the `admin` function's secret. Anon/publishable key, DB password — in `CLAUDE.md`.

# TripKing — Session A (backend lane): edge functions & API

> **This is the entry point for the *backend* parallel session.** Companion to `docs/CONTINUE_HERE.md` (the shared map — read its "Running two sessions in parallel — lane ownership" section first), `CLAUDE.md` (working rules), and `docs/PLATFORM_AND_ADMIN_REQUIREMENTS.md` (the spec). The *frontend* session works from `docs/CONTINUE_HERE_FRONTEND.md` — you do not need to read it.

## Your lane (the only paths you touch)

`supabase/**` (functions, `config.toml`, migrations) · `public/docs/openapi.yaml` + `public/docs/openapi.json` · `scripts/test-*.cjs` + `scripts/db.cjs` · `tests/load/**` (if added) · your own section/bullet in `CLAUDE.md` / `docs/CONTINUE_HERE*.md`.

**Never touch `src/**`** — that's the frontend lane. The only cross-lane contract is the `POST /drivers` / `POST /agents` "create my profile" route below; its shape is fixed here and in `docs/CONTINUE_HERE_FRONTEND.md` — don't change it without updating both docs and pinging the other session.

### Work from the `C:\Apps\TripKing-backend` worktree — ⚠ REMOVED

> **The parallel phase is over.** The `C:\Apps\TripKing-backend` worktree (branch `backend-lane`) has been removed and merged back — work from **`C:\Apps\TripKing`** on `main` (`git pull --rebase origin main` → work → `git push origin main`). This doc is kept as the record of the backend lane's work. **Only** re-create the worktree (`git worktree add -b backend-lane ../TripKing-backend origin/main`; then `npm ci` there) if you genuinely run a backend session concurrently with a frontend session — that's the only reason it existed (so the Husky pre-push gate doesn't trip over the other session's uncommitted WIP). The original setup, for reference:

There was a dedicated git worktree for this lane: **`C:\Apps\TripKing-backend`**, on branch **`backend-lane`** (created with `git worktree add -b backend-lane C:\Apps\TripKing-backend origin/main`; `npm ci` run there). Its working tree was independent of `C:\Apps\TripKing` (where the *frontend* session worked), so the Husky pre-push gate (`tsc --noEmit` + `npm run test:run` + `npm run build`) ran over **its** files only, never the other lane's uncommitted WIP. Push protocol from the worktree was:

```bash
# commit on backend-lane, then:
git fetch origin
git rebase origin/main          # bring backend-lane up to the latest main (clean — disjoint files)
git push origin HEAD:main       # push to remote main; the pre-push gate runs over the clean worktree
```

A rebase conflict ⇒ you crossed lanes — stop, fix the boundary, don't force. (If you ever end up working in `C:\Apps\TripKing` instead and the pre-push gate fails on the other lane's WIP: `git stash push -u -m "…"` it → push → `git stash pop`.)

## State (already done, deployed, pushed)

12 edge functions live at `…supabase.co/functions/v1/<name>`: `/admin` (master-data CRUD; `role=admin` Bearer for mutations — `X-Admin-Key` stopgap removed; audit rows record `actor_user_id`. **Also `/admin/places`** — admin curation of the geocoded `places` table: `GET /admin/places?city_id=&provider=&q=&active=` (incl. inactive), `PATCH /admin/places/:id` (`is_active`/`city_id`/`name`/`formatted_address`/`state`), `POST /admin/places/:id/merge { into }` (dedupe — `merge_places(loser,winner)` SQL fn repoints every `*_place_id` FK then deletes the loser), `DELETE /admin/places/:id` (409 IN_USE if referenced). **And `/admin/users`** — `GET /admin/users?role=&active=&q=`, `GET /admin/users/:id`, `PATCH /admin/users/:id { is_active?, role?, display_name?, reason? }` (the account-level kill switch — `is_active=false` ⇒ `/auth verify-otp`+`refresh` 403; + role grant; can't lock yourself out; audit-logged + an `account_status_change` notification). — `scripts/test-admin-places.cjs` 24/24 ✓ — `scripts/test-admin-config.cjs` master-data + `/admin/users` ✓), `/auth` (phone-OTP — DEV/PLACEHOLDER: `request-otp` always succeeds & returns the dev OTP `12345`; `verify-otp` accepts `12345` or any 4–6 digit; real Supabase sessions; re-sign-in no longer clobbers an existing user's role — 8/8), `/trips` (full lifecycle; **reads (`GET /trips`, `GET /trips/:id`) require a Bearer** — every trip is redacted to the caller via `redactTrip(raw, rel)`: poster/admin → passenger name+phone, poster phone, the assigned driver's live position, `distance_to_destination_km`, the plaintext `passenger_otp`; the assigned driver → passenger name + poster phone + their own position (the passenger phone only when the trip's `hide_passenger_phone` is false; never the OTP); anyone else / an applicant not yet assigned → browse-safe only — no passenger PII, no driver position; `passenger_otp_hash` is never returned. `POST /trips` *requires* `hide_passenger_phone` (bool) + `passenger_count` (int≥1). + `GET /trips/by-otp/:otp` passenger portal (public — the passenger's own view) + `GET /trips/applied` ("my applications", driver; joined trip is browse-safe) + `?assigned_driver_id=me|<uuid>` ("trips I'm driving") + `?near_lat&near_lng&radius_km` radius search (each hit gets `distance_km`) + `from_place`/`to_place` joined, `POST` accepts `from_place_id`/`to_place_id` + fires `alert_match` notifications + **live tracking** (the assigned driver pings `PATCH /drivers/:id/location`; `distance_to_destination_km` on in-progress trips) — 38/38), `/notifications` (6/6), `/vehicles` (CRUD + derived `eligibility_status` + eligibility-dashboard filters — 22/22), `/drivers` + `/agents` (profiles + "create my profile" + `GET /me` + location + admin KYC review + **admin (de)activation** — `PATCH /drivers|/agents/:id/active { is_active, reason? }` flips the `is_active` kill switch (audit-logged + an `account_status_change` notification); a deactivated profile 404s for non-owners, drops out of the default list (`?active=false` / `?include_inactive=true` widen), and `…/me` still returns it with `is_active:false`; `GET /drivers?near_lat&near_lng&radius_km` radius search over the generated `geog`, stale positions ignored — 58/58), `/vacancies` (browse with filters + joins — vacancies of deactivated drivers excluded / post with destinations — 403 if the driver is deactivated / cancel; `current_place`/`vacancy_destinations[].place` joined, `POST` accepts `current_place_id`/`destination_place_ids` + `?near_lat&near_lng&radius_km` radius search + fires `alert_match` — 35/35), `/alerts` (owner-scoped CRUD of saved searches; `from_place_id`/`to_place_id` accepted + joined; evaluated by `match_alerts_for_*` — 21/21), `/reviews` (post-trip ratings; published-or-own visibility; trip-completed check; ratee derived; review_received notification; report + admin moderation — 31/31), **`/analytics` (server-computed — `/analytics/admin` platform dashboard, `/analytics/agent` per-agent, `/analytics/api-metrics` admin latency/error rollup — over the migration-004/006 SQL functions — 17/17)**, **`/places` (maps-backed locations — `GET /places/search` provider-agnostic geocoder proxy, biased to India, no-persist, per-IP rate-limited + cache headers; `POST /places` find-or-create — 9/9; LocationIQ is the live provider, `nominatim` the no-key fallback)**. Backend migrations applied: **001–006 + 011–018** (001 reference data; 002 core schema = `users`+`handle_new_user`+`is_admin()`, `drivers`/`trip_managers`/`vehicles`, `trips`/`trip_acceptances`/`trip_executions` with `driver_payout`/`applicant_count` triggers; 003 = `vacancies`+`vacancy_destinations`/`alerts`/`reviews`/`notifications` + admin-write RLS; 004 = analytics SQL fns; **005 = `rate_limits` + `check_rate_limit()` + `api_metrics`**; **006 = `get_api_metrics_summary`**; **011 = PostGIS + `places` table + nullable `*_place_id` FKs alongside the existing `*_city_id` (BOTH kept) + generated `drivers.geog` — locations Phase B**; **012 = `trips.passenger_otp` (plaintext, alongside the hash)**; **013 = `rate-limits-cleanup` pg_cron job (daily 03:17 UTC)**; **014 = `vacancy_destinations` restructure — surrogate `id` PK, `city_id` nullable, `place_id` allowed, CHECK at-least-one — locations Phase C-2**; **015 = geo SQL fns: `trips_in_radius`/`vacancies_in_radius`/`drivers_in_radius` (ST_DWithin over the GiST-indexed geog, place→city fallback) + `match_alerts_for_trip`/`match_alerts_for_vacancy` (fire `alert_match` notifications) + `alert_notifies_in_app` — locations Phase D**; **016 = `merge_places(loser, winner)` — repoints every `*_place_id` FK then deletes the loser; powers `POST /admin/places/:id/merge`**; **017 = driver/agent deactivation — `is_active`/`deactivated_at`/`deactivation_reason`/`deactivated_by` on `drivers` + `trip_managers` (+ index on `is_active`), `notifications.type` gains `account_status_change`, and `match_alerts_for_trip`/`match_alerts_for_vacancy` re-created to skip alerts whose owner's account (`users.is_active`) or driver/agent profile is deactivated + a deactivated driver's vacancy**; **018 = `api-metrics-cleanup` pg_cron job (weekly, Sun 04:23 UTC — purges `api_metrics` >30 days) + index on `api_metrics(created_at)`**). *(007–010 are the frontend lane's video-KYC migrations — `kyc_documents` / `video_verifications` / `storage_buckets` / `vehicle_plate_photo` — pending on their branch; that's why this lane jumped 006 → 011.)* `verify-otp` (20/phone/10min), the content-create POSTs (`POST /trips` 60/min, `/trips/:id/applicants` 120/min, `/vacancies`/`/reviews`/`/alerts` 30/min), and `GET /places/search` (60/IP/min) + `POST /places` (120/IP/min) are rate-limited via `check_rate_limit`; `withTiming` fire-and-forget-persists every request to `api_metrics` (read it via `GET /analytics/api-metrics`). OpenAPI covers all 12 functions. `supabase/functions/_shared/{cors,timing,supabase,rateLimit,geo}.ts` exist and are reused (`geo.ts` = `parseNearRadius` + `MAX_RADIUS_KM` (100) + `DRIVER_LOCATION_STALE_MINUTES` (30) + `toKm`). The frontend data layer (services/transforms/hooks for the 10 marketplace resources) is already in place — your functions must return shapes its transforms accept (joined `select(...)` strings below); locations epic Phase A (frontend) + C-1 (places types/service/transform/hook + a `LocationSearchPanel`) are on their branches and already consume `/places` + the new `*_place` joins.

## NEXT STEP → **Phase-6 backend — only one decision-blocked item remains: real SMS provider + `auth_otps` for `/auth` — needs a provider decision. Everything else (admin-ops, analytics, hardening, the locations epic backend, the k6 suite, `/admin/places`) is done.** Done so far: the core marketplace API (10 edge fns) + Phase-4 admin-ops + Phase-5 analytics + the `/admin` `role=admin` Bearer migration + the "⬆ Frontend ask" endpoints + trip-manager live tracking + the `/auth` OTP placeholder + **rate limiting (`verify-otp` + the content-create POSTs) + `api_metrics` persistence + `GET /analytics/api-metrics` admin rollup (migrations 005–006) + `CLAUDE.md` literal secrets scrubbed**. (✅ `/drivers`+`/agents` → `supabase/functions/{drivers,agents}/index.ts` + `scripts/test-drivers.cjs`; ✅ `/vacancies` → `supabase/functions/vacancies/index.ts` + `scripts/test-vacancies.cjs`; ✅ `/alerts` → `supabase/functions/alerts/index.ts` + `scripts/test-alerts.cjs`; ✅ `/reviews` → `supabase/functions/reviews/index.ts` + `scripts/test-reviews.cjs`; ✅ Phase-4 admin-ops — KYC review (`PATCH /drivers|/agents/:id/kyc`, CSV `?kyc_status=` queue, `kyc_status_change` notification), reviews moderation (`?flagged=true`, `POST /reviews/:id/moderate`), vehicle-eligibility filters (`?eligibility=`, `?needs_attention=true`); ✅ Phase-5 — `supabase/functions/analytics/index.ts` + `supabase/migrations/004_analytics_functions.sql` + `scripts/test-analytics.cjs`; ✅ Phase-6 step 1 — `/admin` `role=admin` Bearer migration (`X-Admin-Key` removed; `actor_user_id` recorded on audit rows; `test-admin-config.cjs` rewritten) + `docs/SECURITY_REVIEW.md` (RLS-coverage audit + remaining-hardening checklist); ✅ frontend-ask endpoints — `GET /drivers/me`+`GET /agents/me` + public `GET /trips/by-otp/:otp`; ✅ trip-manager live tracking — every trip row joins the assigned driver with live position, in-progress trips get `distance_to_destination_km`, `passenger_otp_hash` stripped everywhere via `enrichTrip`; ✅ `/auth` OTP placeholder — `request-otp` is a no-throw placeholder returning `dev_otp:'12345'`, `verify-otp` accepts `12345` or any 4–6 digit; ✅ Phase-6 polish — rate-limited the content-create POSTs (`POST /trips`/`/trips/:id/applicants`/`/vacancies`/`/reviews`/`/alerts`), migration 006 `get_api_metrics_summary` + `GET /analytics/api-metrics` (admin), scrubbed the `CLAUDE.md` DB-password + dead `ADMIN_API_KEY` literals; ✅ `fix(auth)` — re-sign-in no longer clobbers an existing user's `role` (only NEW users get role seeded; role changes go through `POST /drivers|/agents` or an admin grant); ✅ **maps-backed locations Phase B** — migration 011 (PostGIS + `places` + `*_place_id` FKs alongside `*_city_id` + `drivers.geog`), `supabase/functions/places/index.ts` (`GET /places/search` provider-agnostic geocoder proxy + `POST /places` find-or-create), `scripts/test-places.cjs`, OpenAPI, `.env.example` (`PLACES_PROVIDER`/`PLACES_API_KEY`); deployed. **✅ Geocoder configured — LocationIQ is live**: `PLACES_PROVIDER=locationiq` + `PLACES_API_KEY` (the `pk.…` token) are set as Supabase Edge Function secrets (not committed). Free tier — 5k req/day, 2 req/s — covers a 500–1000-driver scale; bump the LocationIQ plan if usage grows. `nominatim` stays the no-key fallback if the secrets are ever cleared. To switch providers later: `npx supabase secrets set PLACES_PROVIDER=geoapify PLACES_API_KEY=… --project-ref saxcbebqxgatiktsebxw` (no redeploy needed); ✅ **backend round-2 asks** — `GET /trips/:id` echoes the plaintext `passenger_otp` to the poster/admin (migration 012 stores it), `GET /trips?assigned_driver_id=me|<uuid>` ("trips I'm driving"), `GET /trips/applied` ("my applications"), the dead `ADMIN_API_KEY` Supabase secret unset + `.env.example` cleaned, a daily `rate-limits-cleanup` pg_cron job (migration 013); `test-trips.cjs` now 23/23.) ✅ **locations epic Phase C-2 + D** — migration 014 (`vacancy_destinations` surrogate-`id` PK / nullable `city_id` / `place_id` allowed) + 015 (geo SQL fns); `/vacancies`,`/trips`,`/alerts` POST+GET now plumb `*_place_id` (joined `current_place`/`from_place`/`to_place` + `vacancy_destinations[].place`; bad place id → 422); `?near_lat&near_lng&radius_km` radius search (capped 100km) on `GET /trips`/`/vacancies`/`/drivers` (place→city geog fallback; stale driver positions ignored; each hit gets `distance_km`); `POST /trips`/`/vacancies` fire `alert_match` notifications for matching active alerts via `match_alerts_for_*` (geo radii + the trip's rate/commission/car-type/pickup-window filters; owner never notified about own; only `in_app` delivered); `drivers/index.ts` `PATCH /:id/location` auto-stamps `current_location_at`; OpenAPI + smokes updated. ✅ **`GET /trips` PII / driver-position hardening** — `GET /trips` + `GET /trips/:id` now Bearer-required + per-viewer `redactTrip` (poster/admin = everything incl. driver position + OTP; assigned driver = passenger name + poster phone + own position, passenger phone iff `!hide_passenger_phone`, never the OTP; everyone else = browse-safe, no PII, no position); `POST /trips` *requires* `hide_passenger_phone` + `passenger_count`; `by-otp` stays public (the passenger's own view); OpenAPI + `test-trips.cjs` (38/38) + `test-reviews.cjs` updated; see `docs/SECURITY_REVIEW.md`. ✅ **k6 load suite** — `tests/load/{config,helpers,test-data,scenario}.js` + `README.md` + `npm run test:load` (smoke|load presets; per-endpoint p95/p99 thresholds incl. the radius variants + `/places/search`; runs against the deployed API). ✅ **admin `/admin/places` CRUD** — `GET` (browse/filter, incl. inactive) · `PATCH /:id` (`is_active`/`city_id`/`name`/`formatted_address`/`state`) · `POST /:id/merge { into }` (dedupe via the migration-016 `merge_places` SQL fn) · `DELETE /:id` (409 if referenced) — added to the `admin` edge fn (audit-logged); `scripts/test-admin-places.cjs` (24/24) + OpenAPI updated. ✅ **admin can deactivate a driver / agent** (migration 017) — per-profile `is_active`/`deactivated_at`/`deactivation_reason`/`deactivated_by` on `drivers` + `trip_managers`; `PATCH /drivers/:id/active` + `PATCH /agents/:id/active { is_active, reason? }` (admin only — mirrors the `/kyc` route — audit-logged to `admin_audit_log` + an `account_status_change` notification to the affected user). **The flag is enforced everywhere it matters:** `GET /drivers|/agents` default `is_active=true` (`?active=false` / `?include_inactive=true` widen); `GET /drivers|/agents/:id` → 404 for an inactive profile unless the caller is its owner or an admin; `GET /drivers|/agents/me` still returns it (with `is_active:false`, so the frontend can show a "suspended" banner); `GET /vacancies` excludes vacancies of deactivated drivers; `POST /vacancies`, `POST /trips`, `POST /trips/:id/applicants`, `POST /reviews` → 403 `ACCOUNT_SUSPENDED` when the acting driver/agent is deactivated; `POST /trips/:id/assign` → 422 if the chosen acceptance's driver is deactivated; `match_alerts_for_trip`/`match_alerts_for_vacancy` skip inactive alert owners + a deactivated driver's vacancy; `/auth verify-otp` + `/auth refresh` → 403 `ACCOUNT_SUSPENDED` when `users.is_active = false` (the *account*-level kill switch — orthogonal to the *profile* one and to KYC; currently set only via SQL, no admin endpoint yet — a deactivated *profile* can still sign in). In-flight (assigned / in-progress) trips are intentionally left alone. Smoke coverage: `test-drivers.cjs` (the `/active` endpoint + 404-for-strangers + `?active=false`/`?include_inactive=true` + `…/me` returns it), `test-vacancies.cjs` (403 + vacancies hidden), `test-trips.cjs` (apply 403 + assign 422 — also fixed its pre-existing KYC bootstrap so the lifecycle actually runs), `test-reviews.cjs` (review 403 — also fixed its KYC bootstrap). Deployed: `drivers`, `agents`, `vacancies`, `trips`, `auth`, `reviews` — **plus a redeploy of `alerts` and `admin`** which had been stale on the server (their committed `from_place_id`/`to_place_id` and `/admin/places` code had never been deployed); OpenAPI yaml+json updated. ✅ **Phase-6 hardening round 2** — (a) **`PATCH /admin/users/:id`** + `GET /admin/users[?role=&active=&q=]` + `GET /admin/users/:id` on the `admin` edge fn (admin-only): the *account*-level kill switch (`users.is_active` — `verify-otp`+`refresh` 403 when false), role grant/revoke, `display_name`; can't deactivate/demote your own account (422); audit-logged; `account_status_change` notification on an `is_active` flip — fills the gap noted in migration 017 (no SQL needed to suspend an account / promote an admin, except the very first admin). (b) **`role:'admin'` self-signup is now gated by `TRIPKING_DEV_MODE`** — `verify-otp` honours `role:'admin'` only when `TRIPKING_DEV_MODE` is unset/`'true'` (the default, needed by the dev-OTP placeholder + the smoke suites); set `TRIPKING_DEV_MODE=false` (a Supabase secret) in prod and it 403s (admins via `PATCH /admin/users/:id` then). (c) **migration 018** — a weekly `api-metrics-cleanup` pg_cron job (Sun 04:23 UTC, purges `api_metrics` >30 days) + an index on `api_metrics(created_at)`. `scripts/test-admin-config.cjs` extended (the `/admin/users` round-trip: list/view, role grant, deactivate → `verify-otp` 403 → reactivate → `verify-otp` 200, self-lockout guards); deployed `admin` + `auth`; OpenAPI + `docs/SECURITY_REVIEW.md` updated. (Deferred: switching `TRIP_SELECT`'s `*` to an explicit non-PII column allowlist — `redactTrip` already strips PII, so the marginal benefit didn't justify the refactor right now.) **Remaining Phase-6 (the only one): real SMS + `auth_otps` for `/auth` — needs a provider decision — see `docs/SECURITY_REVIEW.md`.** **Locations epic — backend is complete; the frontend follow-up (swap the city `<select>`/chips for `LocationSearchPanel`, send `*_place_id`, carry the joined `*_place` through the domain types/transforms) is on `feat/post-vacancy-availability-window` / `feat/places-ui`.** The recipe below still applies to any new endpoints.

### The recipe (every remaining edge function — mirror `supabase/functions/trips/index.ts`)

1. **`supabase/functions/<name>/index.ts`** — Deno; `serve(withTiming('<name>', async (req) => { … }))`; handle `corsPreflight`; parse the path with `url.pathname.match(/\/<name>(?:\/(.+))?$/)` → routes at the function root (`GET /` = list, `GET /:id`, `POST /:id/<action>`, etc.); for authed routes read the Bearer token (`req.headers.get('authorization')?.slice(7)`), validate via `db.auth.getUser(token)` → `auth.uid()`, look up the caller's `users.role` (+ `drivers.id` if relevant), and **enforce the same ownership rules the RLS policies in migrations 002/003 encode** (owner / poster / assigned-driver / `role==='admin'`); writes go through the **service-role client** (`serviceClient()` from `../_shared/supabase.ts` — it reads the auto-injected `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`); use joined `select(...)` strings that match the frontend transforms (e.g. `'*, from_city:cities!from_city_id(*), to_city:cities!to_city_id(*), car_type:car_types(label)'`); map Postgres errors with a `pgFail` helper (`23505`→`CONFLICT` 409, `23503`→`IN_USE` 409, `23502`/`23514`/`22P02`→`VALIDATION` 422, else `DB_ERROR` 400). Use `ok(data)` / `fail(code, message, status)` from `../_shared/cors.ts`. Reuse `../_shared/{cors,timing,supabase}.ts`.
2. **`supabase/config.toml`** — add `[functions.<name>]` then `verify_jwt = false` (the function does its own auth; the gateway gate is off).
3. **OpenAPI** — append the paths to `public/docs/openapi.yaml` (compact one-line `post: { tags: […], summary: …, … }` style) **and** to `public/docs/openapi.json` (a small `node -e "const p=JSON.parse(…); Object.assign(p.paths,{…}); p.tags=p.tags.concat([…]); fs.writeFileSync(…)"` script, like the trips/auth/notifications/vehicles appends). Add a tag for the resource.
4. **`scripts/test-<name>.cjs`** — Node smoke test mirroring `scripts/test-vehicles.cjs`: read `<NAME>_API_BASE` (skip cleanly + `exit 0` if unset); get a token via `POST /auth/auth/request-otp` then `POST /auth/auth/verify-otp { phone, otp:'123456', display_name, role }`; exercise the routes (a public read → 200+array, a write without auth → 401, a `404` case, a `403` case where applicable); `process.exit(1)` on any failed check.
5. **Deploy & smoke:** `npx supabase functions deploy <name> --project-ref saxcbebqxgatiktsebxw --no-verify-jwt` then `<NAME>_API_BASE=https://saxcbebqxgatiktsebxw.supabase.co/functions/v1 node scripts/test-<name>.cjs`.
6. **Commit & push (from the `C:\Apps\TripKing-backend` worktree, branch `backend-lane`):** `feat(<name>): …` (one logical change; review `git diff --stat` — must be only `supabase/`, `public/docs/`, `scripts/`, `tests/load/`, and your own bullet in the shared docs) → `git fetch origin && git rebase origin/main && git push origin HEAD:main` (the Husky pre-push runs `tsc --noEmit` + `npm run test:run` + `npm run build` over the clean worktree — all green; secret-scanning blocks any literal `sb_secret_…` so never commit one).

### Per-resource notes

- **`/drivers` (+`/agents`) — ✅ SHIPPED (commit `eccde0e`)** — `GET /drivers ?current_city_id=&kyc_status=&limit=` (public; joins `home_city`/`current_city`/`vehicles`(summary)), `POST /drivers` ("create my profile" — `user_id = auth.uid()`; body `{ full_name, role:'driver'|'trip_manager', home_city_id, current_city_id, phone, email, profile_photo_url }`; `role:'trip_manager'` creates a `trip_managers` row instead; **idempotent** — returns the existing profile if one exists; syncs `users.role`), `GET /drivers/:id`, `PATCH /drivers/:id` (owner or admin — `full_name`/`email`/`home_city_id`/`current_city_id`/`profile_photo_url`), `PATCH /drivers/:id/location` (owner — `current_city_id`/`current_lat`/`current_lng`/`current_location_at`). `/agents` (= `trip_managers`): `GET /agents ?business_city_id=&kyc_status=&limit=` (public), `POST /agents` (create my agent profile; idempotent; syncs `users.role`), `GET /agents/:id`, `PATCH /agents/:id` (owner or admin — `full_name`/`email`/`business_name`/`business_city_id`/`profile_photo_url`). Frontend service `src/lib/api/services/drivers.ts` already calls `/drivers`, `/drivers/:id`, `/drivers/:id/location`, `/agents/:id` (and Session B adds `createMyDriverProfile`/`createMyAgentProfile` → `POST /drivers`/`POST /agents`); transform `src/lib/api/transforms/driver.ts`. Files: `supabase/functions/drivers/index.ts`, `supabase/functions/agents/index.ts`, `scripts/test-drivers.cjs` (22 checks ✓).
- **`/vacancies` — ✅ SHIPPED** — `GET /vacancies ?current_city_id=&destination_city_id=&status=&driver_id=&limit=` (public; joins `driver`(summary)+`vehicle`(summary)+`current_city`+`vacancy_destinations(city:cities(*))`; `destination_city_id` filter pre-resolves `vacancy_destinations` → `id IN (...)`), `GET /vacancies/:id`, `POST /vacancies` (driver — `current_city_id` required; body incl. `destination_city_ids: string[]` → insert the vacancy then the junction rows, rollback the orphan on a junction-insert failure), `POST /vacancies/:id/cancel` (owning driver/admin — `status='cancelled'`, 409 if already cancelled). Files: `supabase/functions/vacancies/index.ts`, `scripts/test-vacancies.cjs` (22 checks ✓). Service `services/vacancies.ts`; transform `transforms/vacancy.ts`.
- **`/alerts` — ✅ SHIPPED** — owner-scoped (every route Bearer-required): `GET /alerts` (the caller's, joins from/to city), `GET /alerts/:id` (owner/admin), `POST /alerts` (`user_id = caller`; `from_city_id` required; whitelisted writable columns), `PATCH /alerts/:id` (owner; incl. `is_active`/`paused_at` to pause/resume), `DELETE /alerts/:id` (owner; hard delete). `notify_via text[]` is CHECK-constrained to `<@ {push,sms,email,in_app}` → bad values 422; `car_type_ids uuid[]`. Files: `supabase/functions/alerts/index.ts`, `scripts/test-alerts.cjs` (21 checks ✓). Service `services/alerts.ts`; transform `transforms/alert.ts`.
- **`/reviews` — ✅ SHIPPED** — `GET /reviews ?trip_id=&ratee_user_id=&direction=&limit=` (visibility: admins see all; an authed caller sees `is_published` rows + their own (rater/ratee) via a PostgREST `.or(...)`; anon sees published only), `GET /reviews/:id` (same visibility), `POST /reviews` (Bearer — `rater_user_id = caller`, `rater_role = caller.role`; requires `trip_id`/`direction`(in the 3-value set)/`score`(int 1-5); the trip must exist and be `status='completed'` (422 otherwise); `ratee_user_id` derived from the trip when omitted — `posted_by_user_id` for `driver_to_manager`, else the assigned driver's `user_id`; unique `(trip_id, direction)` → 409; fires a `review_received` notification for the ratee), `POST /reviews/:id/report` (any authed user — sets `is_flagged`+`flag_reason`). Anonymous passenger reviews (`rater_user_id` null) remain out of scope here. Files: `supabase/functions/reviews/index.ts`, `scripts/test-reviews.cjs` (25 checks ✓ — bootstraps a full post→apply→assign→OTP-start→complete trip first). Service `services/reviews.ts`; transforms `transforms/review.ts` (`transformReview` + `transformNotification`).

### Phase-4 backend (admin-ops) — ✅ DONE (commit `feat(admin-ops): …`)

Implemented as routes on the existing functions (admin-only via a real `role=admin` Bearer check — `verify-otp` mints admin sessions in dev with `role:'admin'`):
- **KYC review** — `GET /drivers ?kyc_status=docs_submitted,video_pending` (CSV → the queue), `GET /agents ?kyc_status=…`; `PATCH /drivers/:id/kyc { kyc_status, note? }` and `PATCH /agents/:id/kyc { … }` (admin; validates the 6-value set; fires a `kyc_status_change` notification to the subject's user). (The "video console" is a frontend-only video-call UI — no backend.)
- **Reviews moderation** — `GET /reviews ?flagged=true` (admin → the `is_flagged` queue); `POST /reviews/:id/moderate { is_published?, clear_flag? }` (admin; records `moderated_by`/`moderated_at`).
- **Vehicle-eligibility dashboard** — `GET /vehicles ?eligibility=expired,expiring_soon` (CSV filter on the derived status) and `?needs_attention=true` (eligibility_status ≠ eligible).
- **Translation manager** — already covered by `/admin/languages` (the §7.7 config UI's "Languages" list); no new endpoint.

Smoke coverage extended in `scripts/test-drivers.cjs` (KYC), `scripts/test-reviews.cjs` (moderation), `scripts/test-vehicles.cjs` (eligibility filters + full vehicle CRUD).

### Phase-5 backend (analytics) — ✅ DONE (commit `feat(analytics): …`)

`supabase/functions/analytics/index.ts` — thin Bearer-gated wrappers over the migration-004 SQL functions: `GET /analytics/admin` (admin only → `get_admin_dashboard()`, a jsonb blob: users by role, drivers/agents by KYC, vehicles by eligibility, trips by status, completed-fare/commission/payout sums, vacancies by status, active alerts, flagged-review count, unread-notifications count, a 6-month trip series), `GET /analytics/agent` (the caller's own; `?user_id=` is admin-or-self → `get_agent_analytics(uuid)`: trips posted/by-status, fare totals, applicants received, unique drivers used, reviews received + avg score, a 6-month series). `supabase/migrations/004_analytics_functions.sql` (both `stable security definer set search_path = public`, granted to `authenticated`/`anon`/`service_role`). `scripts/test-analytics.cjs` (12 checks ✓). `[functions.analytics] verify_jwt = false` in `config.toml`. No frontend transform yet — Phase-5 frontend will add `useAnalytics` + a transform for the blob shapes.

### Phase-6 backend — status

- ✅ **`/admin` `role=admin` Bearer auth** — `X-Admin-Key` stopgap removed; mutations require a Bearer whose `users.role === 'admin'`; `admin_audit_log` rows now record `actor_user_id`. (Side effect: the `/administration/config` UI's writes now work when an admin is signed in.) `scripts/test-admin-config.cjs` rewritten (Bearer; 11/11 ✓).
- ✅ **RLS-coverage review** — `docs/SECURITY_REVIEW.md` (edge-function check ↔ RLS-policy table; no gaps; two intentional divergences documented; remaining-hardening checklist).
- ✅ **Frontend-ask endpoints** — `GET /drivers/me` + `GET /agents/me` (Bearer → the caller's own `drivers`/`trip_managers` row, joined like `GET /:id`; 404 if none); public `GET /trips/by-otp/:otp` (hashes the OTP, finds the matching trip — any status with the hash set, latest `assigned_at` first — joins the assigned driver+vehicle; fare fields nulled when `show_fare_to_passenger` is false; `passenger_otp_hash` never echoed — also stripped from `GET /trips/:id`).
- ✅ **Trip-manager live tracking** — every trip row now joins the assigned driver with `current_lat/current_lng/current_location_at`; for an `in_progress` trip with a known driver position + destination coords the row also gets `distance_to_destination_km` (haversine). A manager's live map = `GET /trips?status=in_progress&posted_by_user_id=<me>`. The assigned driver pings `PATCH /drivers/:id/location` while running the trip (existing route). (Note: `GET /trips` is public, so driver positions are public — same posture as the already-public trip list; see the follow-up in `docs/SECURITY_REVIEW.md`.) `test-trips.cjs` now exercises the full lifecycle incl. start → driver-location-ping → in-progress-trip-has-position+distance → the live-trips list → complete (15/15 ✓).
- ✅ **`/auth` placeholder + dev OTP `12345`** — `request-otp` is a no-throw placeholder that returns `dev_otp: '12345'`; `verify-otp` accepts `12345` or any 4–6 digit code. (`test-auth.cjs` green; the other smokes still pass with `'123456'` since `/^\d{4,6}$/` matches it.)
- ⬜ **Real SMS provider + an `auth_otps` table for `/auth`** (drop the dev shortcuts; gate `role:'admin'`) — needs a provider decision (Twilio / MSG91 / …). This is the implementation behind the `request-otp` placeholder.
- ✅ **Rate limiting (infra + `verify-otp` + the content-create POSTs + `/places`)** — `rate_limits` table + `check_rate_limit()` SQL fn (migration 005) + `_shared/rateLimit.ts` (`rateLimitOk()`, fails open); `POST /auth/verify-otp` 20/phone/10min, per-user limits on `POST /trips` (60/min) · `POST /trips/:id/applicants` (120/min) · `POST /vacancies`/`/reviews`/`/alerts` (30/min), per-IP limits on `GET /places/search` (60/min) · `POST /places` (120/min) — over the limit ⇒ 429 `RATE_LIMITED`. ✅ A daily pg_cron job purges stale buckets (`rate-limits-cleanup`, 03:17 UTC, migration 013). TODO: extend to `request-otp` once real SMS lands.
- ✅ `passenger_otp_hash` is stripped from every `/trips` response (`enrichTrip`, applied in the list, `fullTrip`, `GET /:id`, `by-otp`); the plaintext `passenger_otp` (stored on assign — migration 012) is also stripped by default and re-attached on `GET /trips/:id` **only for the poster (`auth.uid() = posted_by_user_id`) or an admin** — so a poster can re-open the `/passenger/<otp>` portal link any time. (Optional follow-up: switch `TRIP_SELECT`'s `*` to an explicit column list, dropping passenger PII by default.)
- ✅ **`api_metrics` persistence + admin read endpoint** — `api_metrics` table (migration 005) + a fire-and-forget insert from `withTiming()` (memoized service-role client; never awaited; ~90% persist on Deno Deploy — verified populating: `auth`/`drivers`/`trips`/`agents`/`admin` rows after the smokes). All functions re-deployed. `get_api_metrics_summary(p_hours)` SQL fn (migration 006) → `GET /analytics/api-metrics?hours=24` (admin only; per-endpoint count / errors (status≥500) / avg / max / p95 + window bounds; `test-analytics.cjs` covers it).
- ✅ **`tests/load/` k6 suite** — `config.js` (BASE_URL + `smoke`/`load` presets + global & per-endpoint p95/p99 thresholds), `helpers.js` (`signIn(role)` via the dev-OTP path, `createDriverProfile`, `hit(name,method,path,{token,body})` recording `ep_<name>_duration`/`_errors`/`_reqs`, the `ENDPOINTS` list — 429s under load go to `rate_limited_429`, not failures), `test-data.js` (the weighted mix — public reads heavy + a couple authed reads incl. `GET /trips`, the `near_*` radius variants, `/places/search`, a light `POST /places`), `scenario.js` (the k6 entry), `README.md`. `npm run test:load` runs the smoke preset; `k6 run -e SCENARIO=load -e VUS=… -e DURATION=… -e BASE_URL=… tests/load/scenario.js` for a real run. (No separate load-test env — `BASE_URL` defaults to QA.) **New edge fn ⇒ add it to `helpers.ENDPOINTS` + `config.ENDPOINT_THRESHOLDS` + `test-data.getRequests()`.**

(The frontend session builds the UIs on these later — coordinate the response shapes via OpenAPI, which you own.)

## Known TODOs / gotchas

- **`/admin/*` auth** — ✅ migrated: mutations require a `role=admin` Bearer (no more `X-Admin-Key`); the `ADMIN_API_KEY` Supabase function secret has been **unset** and the `.env.example` placeholder dropped (the `.env.development` entry, if any, is dead — gitignored, harmless). Reads stay public. The `/administration/config` UI's writes work when an admin is signed in.
- **dev-OTP / `/auth` placeholder** — `request-otp` is a no-op placeholder: it always succeeds (never throws) and returns the dev OTP `12345` in `dev_otp` so the client can auto-fill it. `verify-otp` accepts `12345` (the default) or any 4–6 digit code, **and honours `role:'admin'`** (handy for dev + the smoke tests; MUST be gated for prod — see `docs/SECURITY_REVIEW.md`). The session itself is real. Wiring a real SMS provider + an `auth_otps` table (and dropping these dev shortcuts) is the remaining Phase-6 item that needs a provider decision.
- **Driver/agent profiles on sign-in** — `verify-otp` creates the `users` row only; the `POST /drivers` / `POST /agents` "create my profile" route (✅ shipped) is how a fresh sign-in becomes a driver/agent. The `/trips` apply→assign→start and `/vehicles` POST paths can now be smoke-tested end-to-end (their `scripts/test-*.cjs` haven't been upgraded to do so yet — optional follow-up: bootstrap a driver via `POST /drivers` in `scripts/test-trips.cjs` / `scripts/test-vehicles.cjs`).
- **⬆ Frontend ask (Session B) — ✅ DELIVERED.** `GET /drivers/me` + `GET /agents/me` (Bearer → the caller's own profile, joined; 404 if none — unblocks driver home / agent home / profile + the driver-side apply/assigned-trip actions) and public `GET /trips/by-otp/:otp` (the passenger portal — joins the assigned driver+vehicle; fare nulled when `show_fare_to_passenger` is false; `passenger_otp_hash` never echoed). Note re `hide_passenger_phone`: it's the *driver-side* privacy setting (hides the passenger's phone in `GET /trips/:id`), so it doesn't gate anything in the passenger's own view — the by-otp response includes the assigned driver's `phone` so the passenger can coordinate. If Session B needs a different gating, say so.
- **⬆ Frontend ask (Session B, round 2) — ✅ DELIVERED:**
  1. ✅ **Passenger-OTP echo on `GET /trips/:id`.** The plaintext OTP is now stored on assign (migration 012 `trips.passenger_otp`) and echoed on `GET /trips/:id` **only when the caller is the poster (`auth.uid() = posted_by_user_id`) or an admin** — never to anyone else, never the hash. `transformTrip` already maps `api.passenger_otp` → `passengerOtp`, so the share link works for the poster any time now. (Pre-existing trips have only the hash → their poster sees `undefined` until they re-assign.)
  2. ✅ **"Trips I'm driving"** — `GET /trips?assigned_driver_id=<driver-uuid>` (public) or `GET /trips?assigned_driver_id=me` (Bearer → resolves the caller's driver profile; `[]` if none). Combine with `&status=assigned,in_progress` for the driver-home "you have N trips to drive" card.
  3. ✅ **"My applications"** — `GET /trips/applied` (Bearer; driver-scoped) → the caller's own `trip_acceptances`, newest first, each with its joined trip. The `myApplicationsStore` localStorage shim can be replaced by this (it's now device-independent).
- **⬆ Frontend ask (round 3) — passenger registry (please pick up):** every trip already captures `passenger_name` + `passenger_phone`; turn that into a first-class **passengers** directory so we know who first registered each passenger.
  1. **Migration `NNN_passengers.sql`** — `passengers`: `id` (uuid), `phone` text **unique** (E.164), `name` text, `aliases` text[] default `'{}'` (other names the phone has been entered under, excluding the canonical `name`), `referred_by_user_id` uuid FK `users` (the poster of the first trip with this phone — set once, never changed), `first_trip_id` uuid FK `trips`, `first_seen_at` timestamptz, `trips_count` int default 0, `created_at`/`updated_at`. RLS: admin reads all (the upsert path uses the service role); decide whether agents may read their own referred rows (frontend is admin-only for now, so not required).
  2. **`POST /trips`** — after the trip insert, upsert the passenger: `INSERT INTO passengers (phone, name, referred_by_user_id, first_trip_id, first_seen_at, trips_count) VALUES (<phone>, <name>, <posted_by_user_id>, <new trip id>, now(), 1) ON CONFLICT (phone) DO UPDATE SET trips_count = passengers.trips_count + 1, aliases = (CASE WHEN EXCLUDED.name = passengers.name OR EXCLUDED.name = ANY(passengers.aliases) THEN passengers.aliases ELSE array_append(passengers.aliases, EXCLUDED.name) END), updated_at = now()` — **first name wins** (the canonical `name` and `referred_by_user_id` are never overwritten); later trips with a different name append to `aliases`. (If trip-create can fail validation after the trip insert, do the upsert *after* the trip is committed.)
  3. **`supabase/functions/passengers/index.ts`** — `GET /passengers` (admin Bearer) — list, newest-first by `first_seen_at`, with `?q=` (name/phone ilike), `?sort=`, pagination; **join the referrer** as `referred_by:users!referred_by_user_id(id, display_name, role, phone)`. And `GET /passengers/lookup?phone=<e164>` (Bearer; agents/drivers) — the passenger row (+ `referred_by`) for that exact phone, or **404** if none — used by the post-trip form to prefill the name. Audit-log nothing here (read-only). Per the recipe: `withTiming`, OpenAPI, `scripts/test-passengers.cjs`, k6 entry, regression smoke.
  Frontend half (mine — building now against this contract): `types/passenger.ts` + `transforms/passenger.ts` + `services/passengers.ts` + `usePassengers`/`useLookupPassengerByPhone`; the post-trip phone-blur lookup-and-prefill with a "checking…" spinner; an `/administration/passengers` page + tile. (Until the endpoints deploy the admin page renders `<ErrorState>` and the lookup no-ops/404s — nothing of yours is blocked.)
- **Edge-function routing** — function `<name>` is reachable at `…/functions/v1/<name>/…`; routes live at the function root (`GET /` = list, `GET /:id`, …). The `auth` function additionally matches the *last* path segment, so `/auth/me` and `/me` both resolve. (An `api.tripking.in` gateway would map `/<name>/*` → the `<name>` function later.)
- **`supabase/` is outside the app build** — `tsc --noEmit` / ESLint / `vite build` ignore it, so your edge-function changes don't affect the `tsc/test/build` gate; verification for them is the `scripts/test-<name>.cjs` smoke against the deployed function. (This is also why your lane and the frontend lane never break each other's pre-push gate.)

## Keys & access (full details in `CLAUDE.md` — never ask the user for tokens)

Supabase project `tripking` ref `saxcbebqxgatiktsebxw`. SQL / migrations via `node scripts/db.cjs "<SQL>"` or `node scripts/db.cjs --file supabase/migrations/<NNN>_<name>.sql` (Management API; access token from the Supabase CLI's Windows Credential Manager entry — if missing, `npx supabase login`). Deploy with `npx supabase functions deploy <name> --project-ref saxcbebqxgatiktsebxw --no-verify-jwt`. Service-role secret key + legacy `service_role` JWT in `.env.development` (`SUPABASE_SERVICE_ROLE_KEY`) — never commit the literal value (GitHub push-protection blocks `sb_secret_…`); edge functions get `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` auto-injected so no secret-setting is needed there. `/places` geocoder: `PLACES_PROVIDER`=`locationiq` + `PLACES_API_KEY` are set as Supabase function secrets (`npx supabase secrets set …`). (`ADMIN_API_KEY` is gone.) Anon/publishable key — in `CLAUDE.md`; DB password — in `.env.development` (no longer in `CLAUDE.md`).
